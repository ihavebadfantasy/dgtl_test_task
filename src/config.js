const PORT = 3000;

const LOGS_DIR_PATH = `${__dirname}/events`;
const DATE_IN_LOG_FILE_NAME_START = 9;
const DATE_IN_LOG_FILE_NAME_END = 19;

module.exports = {
  PORT,
  LOGS_DIR_PATH,
  DATE_IN_LOG_FILE_NAME_START,
  DATE_IN_LOG_FILE_NAME_END,
};
